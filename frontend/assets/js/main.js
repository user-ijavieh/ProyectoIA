/**
 * Punto de entrada principal de la aplicación del tablero
 */

document.addEventListener('DOMContentLoaded', () => {
    // Inicializar componentes
    BoardRenderer.init();
    TicketManager.init();
});
